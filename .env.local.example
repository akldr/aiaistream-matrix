# TTS API Endpoint
# For Cloudflare Pages: Set in Cloudflare Dashboard (Pages > Settings > Environment Variables)
# For local development: use your Worker URL or local API
# NEXT_PUBLIC_TTS_API_ENDPOINT=https://your-worker-subdomain.workers.dev/api/tts-log

# Log Access Token (for reading logs via GET requests)
# Generate with: openssl rand -base64 32
# Set in Worker via: wrangler secret put LOG_ACCESS_TOKEN --env production
# LOG_ACCESS_TOKEN=your-secure-token-here

# Note: NEVER commit actual .env.local file to git
# This is just an example. Copy to .env.local and fill in your values.
